cd ~/DE_project
source venv/bin/activate

When you want to deactivate
deactivate

# Terminal 1
cd /opt/kafka
bin/zookeeper-server-start.sh config/zookeeper.properties

# Terminal 2
cd /opt/kafka
bin/kafka-server-start.sh config/server.properties

python3 kafkaProducer.py

spark-submit --jars /usr/share/java/postgresql.jar sparkConsumer.py

sudo -u postgres psql
psql -U postgres -d postgres -h localhost -W

sudo service postgresql start
kafka-topics.sh --list --bootstrap-server localhost:9092

spark-submit \
  --packages org.apache.spark:spark-sql-kafka-0-10_2.13:3.5.6 \
  --jars ~/jars/postgresql-42.6.0.jar \
  sparkConsumer.py
  
sudo -u postgres psql -d postgres
SELECT * FROM kafka_stream LIMIT 10;





